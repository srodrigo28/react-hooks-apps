export type Movie = {
    titulo: string;
    avatar: string;
}

export type User = {
    name: string;
    email: string;
    senha: string;
}

export type contas = {
    nome: string;
    vencimento: string;
    valor: string;
}